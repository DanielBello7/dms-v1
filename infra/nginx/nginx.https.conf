## nginx.https.conf

## REDIRECT -----------------------------------------------------------------------------------------
# https://dms.danielbello.dev || https://dmsapi.danielbello.dev
server {
  listen 80 default_server;
  listen [::]:80 default_server;

  server_name dms.danielbello.dev dmsapi.danielbello.dev

  location /.well-known/acme-challenge/ {
    root /var/www/certbot;
  }

  location / {
    return 301 https://$host$request_uri;
  }
}

## FRONTEND ----------------------------------------------------------------------------------------
# WEB
# https://dms.danielbello.dev
server {
  listen 443 ssl;

  ssl_certificate /etc/letsencrypt/live/dms-subdomains/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/dms-subdomains/privkey.pem;

  server_name dms.danielbello.dev www.dms.danielbello.dev;
  large_client_header_buffers 4 32k;

  location / {
    resolver 127.0.0.11 valid=30s;
    set $backend "dms-web:3000";

    proxy_pass http://$backend;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_cache_bypass $http_upgrade;

    proxy_next_upstream error timeout invalid_header http_502 http_503;
  }
}

## BACKEND -------------------------------------------------------------------------------------------
# API
# https://dmsapi.danielbello.dev
server {
  listen 443 ssl;

  ssl_certificate /etc/letsencrypt/live/dms-subdomains/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/dms-subdomains/privkey.pem;

  server_name dmsapi.danielbello.dev www.dmsapis.danielbello.dev;
  large_client_header_buffers 4 32k;

  location / {
    resolver 127.0.0.11 valid=30s;
    set $backend "dms-api:3001";

    proxy_pass http://$backend;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_cache_bypass $http_upgrade;
    proxy_next_upstream error timeout invalid_header http_502 http_503;
  }
}